# Defects, reproductions and corrections

`reproduce_baseline.py` reads immutable b2ea605 library bytes into a temporary directory. Its exact observed outputs are in baseline-counterexamples.json. It reproduces: no actual main-ref request; a moved PR head paired with the expected commit tree; missing GraphQL connection classified OK/complete/empty; ruleset HTTP 200 `{}` classified OK; REST HTTP 200 object classified complete; and endpoint-required administration header alone generating a classic absence claim.

Corrections are confined to the independent v2 collector. Actual refs and expected identities are separate; before/after main, PR head/base and frozen ref SHA/tree observations carry request IDs, HTTP errors, shape errors and expected mismatches. PR lists now paginate. Source completeness requires validated shapes, terminal pagination, unique identities and stable GraphQL totalCount. Partial rules remain visible with independent completeness flags. Ruleset bypass omissions, missing allowance nodes/cursors/counts and unsupported rule schemas remain gaps. Timeout/OSError returns are recorded rather than aborting packet generation. Existing evidence output directories are refused.

The original working directory is preserved at tooling/3a59255, tree 54d81ed; its .omo and Python caches were not reset or removed. The remote tooling branch remains b2ea605, tree 71df498. The original local main ref is stale at b1e035d07d94364138feedd34bda730dba70fb93; remote main is 22665d7. This new worktree starts directly from b2ea605, whose parent is the verified remote main. No reset, cherry-pick, rebase or history rewrite was used.

Baseline test availability: b2ea605 contains no tests/fixtures, so the requested discovery command exits 1 (ImportError). The existing local 3a59255 suite has 30 passing tests. Those same-repository tests/fixtures were brought into the corrective branch. Fixture additions model the now-observed REST refs and schema fields (resetAt, viewerPermission, allowance counts/endCursor/actor IDs, repository fork/admin, classic URL). The clean user-owned main-only scenario no longer invents an inherited org ruleset; a separate contradiction case explicitly requires UNKNOWN. Original error tests now assert PARTIAL coverage while separately retaining positive rules, rather than discarding proven presence merely because another source failed. The tool status test expects a corrective candidate requiring independent review, not a self-attested implementation verdict.

The original suite called gh auth status through environment capture. Corrective offline tests stub that capture so tests perform no gh/network operation. Real transport safety is separately tested with subprocess mocks. The saved live schema is tested offline, including rejection of the old reset field.

Additional bounded fail-open fix: the old pattern_scope tested a few example names and could classify ma?n as main-only. Complex wildcard scope now remains UNKNOWN rather than claiming absence from sampling. Pattern language completeness is not claimed. Exact main and documented ~DEFAULT_BRANCH remain decidable.

Remaining deliberate limits: allowance connections beyond 20 are incomplete, matchingRefs is count-only, unsupported parameterized rule types remain incomplete, organization/enterprise export outside established personal non-fork scope remains UNKNOWN, and sequential protection observations are not an atomic snapshot. No result authorizes PR #11 adoption.

Historical boundaries: INPUT_MISSING evidence is untouched. PR #13 native packet/run 34205089339 and artifact 10047503719 were not re-collected or reimplemented. Historical run 33967073453 remains NOT_OBTAINED as supplied by the user; this task makes no recovery claim. Trusted run 34205091382 MODULE_NOT_FOUND remains an existing stacked-base/governance adoption issue; PR #13 and trusted rules were not altered.
